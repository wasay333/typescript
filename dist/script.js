"use strict";
// so both things are merge together in memory
function abcd(human) {
    human.name.concat();
    human.age.toExponential(2);
}
function getMethaFood(mithai) {
    mithai.name;
    mithai.price;
    mithai.verySweet;
}
function getKhattaFood(khattai) {
    khattai.name;
    khattai.price;
    khattai.verySour;
}
// so basically methafood has one and other it get from food which is being extended
